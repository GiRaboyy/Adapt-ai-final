# ── Public variables (exposed to browser) ────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# ── Server-only variables (never exposed to browser) ─────────────────────────
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ── Environment identifier ───────────────────────────────────────────────────
ENVIRONMENT=local

# ── Git commit SHA (optional, auto-injected by Vercel) ───────────────────────
GIT_SHA=local-dev

# ── Yandex AI Studio (required for training generation) ──────────────────────
YANDEX_API_KEY=your-yandex-api-key
YANDEX_FOLDER_ID=your-yandex-folder-id
YANDEX_PROMPT_ID=your-yandex-prompt-id
# Optional:
# YANDEX_PROJECT_ID=your-yandex-project-id
# YANDEX_MODEL_URI=yandexgpt-lite/latest
