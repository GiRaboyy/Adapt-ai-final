/** @type {import('next').NextConfig} */
const nextConfig = {
  // Vercel will handle Python functions in api/ directory
  // No special rewrites needed as Vercel automatically routes /api/* to api/index.py
}

module.exports = nextConfig
